export enum TransactionType {
    CREDIT = "credit",
    DEBIT = "debit",
    TRANSFER = "transfer",
}

export enum TransactionStatus {
    COMPLETED = "completed",
    FAILED = "failed",
    PENDING = "pending",
    REVERSAL = "reversal"
}